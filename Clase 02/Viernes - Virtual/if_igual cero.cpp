#include <iostream>
using namespace std;

/**
Operadores de Relacion
   > mayor que
   >= mayor o igual que
   < menor que
   <= menor o igual que
   == igual que
   != distinto que

*/

int main() {
   int numero;
   
   cout << "Ingrese numero: ";
   cin >> numero;
   
   if(numero == 0){
      cout << "El numero es cero" << endl;
   }
   
	return 0;
}

